# v

```
apt update && apt upgrade -y && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/javakeisha/bayer/main/setup.sh && chmod +x setup.sh && ./setup.sh
```